#ifndef VALIDATION_REPLACE_H
#define VALIDATION_REPLACE_H
using namespace std;
#include <string>

bool validation(int n, char old_value, const string& text);

string replace(string text, int n, char old_value, char new_value);

#endif
